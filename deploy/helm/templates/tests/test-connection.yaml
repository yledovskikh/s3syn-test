apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "s3syn-test.fullname" . }}-test-connection"
  labels:
    {{- include "s3syn-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "s3syn-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
