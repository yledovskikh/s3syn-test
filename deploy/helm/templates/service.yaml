apiVersion: v1
kind: Service
metadata:
  name: {{ include "s3syn-test.fullname" . }}
  labels:
    {{- include "s3syn-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "s3syn-test.selectorLabels" . | nindent 4 }}
